// Root component. Just imports and displays the context_state_config.js component.

import React from 'react';
import ContextState from './context_state_config';

const App = () => {
    return(
        <div>
            <ContextState />
        </div>
    )
}

export default App;